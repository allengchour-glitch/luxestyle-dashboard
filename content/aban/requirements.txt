# aban news Pro — optionale Extras (Engine laeuft auch ohne, mit stdlib-Fallback).
# Installation (Debian/Ubuntu setuptools-Workaround):
#   SETUPTOOLS_USE_DISTUTILS=stdlib pip install -r requirements.txt
feedparser>=6.0
trafilatura>=1.6
langdetect>=1.0.9
